# Netlify build config for the Trooth React app.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every path serves index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
